{
  "name": "get-link-3d66",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20.18 <21"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "drive:auth": "npm run drive:auth --prefix backend",
    "drive:check": "npm run drive:check --prefix backend",
    "seed:models": "npm run seed:models --prefix backend",
    "lint": "eslint backend/server.js backend/src backend/test frontend/src",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "concurrently": "^9.2.3",
    "eslint": "^9.39.2",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "globals": "^16.5.0"
  }
}
